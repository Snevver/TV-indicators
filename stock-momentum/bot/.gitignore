# Local run state — yours, not the repo's.
state.json
rebalances.csv
history.csv
latest.json
state.json.tmp

# Hand-made backups from a cleanup — yours, not the repo's.
*.bak
# Broker metadata cache: ~16k rows, refetched on demand.
instruments.json

# The smoke test is a local experiment, not the repo's.
smoke.json
smoke.json.tmp

# This month's batch of orders: a live record of what was sent to the broker
# and what came back. Never the repo's business, and it holds order ids.
pending.json
pending.json.tmp
pending-demo.json
pending-demo.json.tmp

# Dated cash-in events and the hourly value snapshots the tracker writes.
deposits.csv
deposits.csv.tmp
hourly.csv
hourly.csv.tmp
