# Local run state — yours, not the repo's.
state.json
rebalances.csv
history.csv
latest.json
state.json.tmp

# Hand-made backups from a cleanup — yours, not the repo's.
*.bak
# Broker metadata cache: ~16k rows, refetched on demand.
instruments.json

# The smoke test is a local experiment, not the repo's.
smoke.json
smoke.json.tmp
